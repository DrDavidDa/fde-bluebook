# FDE 工程师蓝皮书 · 网站部署说明（给 IT/运维）

> 交付日期：2026-08-12
> 联系人：DR.David（产品）
> 线上参考站（当前效果）：https://drdavidda.github.io/fde-bluebook/

---

## 一、这是什么

一个**纯静态网站**（HTML + CSS + JS + 图片），共 27 个文件、约 1.3 MB。

- **无后端**：不需要数据库、不需要 Node/PHP/Java 运行时，任何静态服务器都能挂
- **无外部依赖**：所有 JS/字体/图表库（ECharts）均已本地化，内网/离线环境也能跑
- **无用户数据上传**：阅读进度、测评结果只存访客浏览器 localStorage，不产生任何服务端数据，无隐私合规负担

## 二、部署步骤（3 步）

1. 把 `site/` 目录下的**全部文件**上传到 Web 服务器的站点根目录（或某个子路径）
2. 按第四节改一下 404 配置（可选但建议）
3. 如果用公司自己的域名，跑一遍第三节的域名替换脚本（只影响 SEO/分享效果，不影响功能）

入口文件是 `index.html`，其余页面通过相对路径互相链接，**挂在根目录或任意子目录都可以**。

## 三、域名替换（部署到公司域名时执行）

页面 `<head>` 里的 canonical / og:url / og:image 以及 `sitemap.xml`、`robots.txt`
目前写的是 GitHub Pages 地址。换成公司域名只需一次全局替换，已提供脚本：

**Windows 服务器（PowerShell）：**

```powershell
.\replace-domain.ps1 -NewBase "https://your.company.com/fde/"
```

**Linux 服务器（bash）：**

```bash
./replace-domain.sh "https://your.company.com/fde/"
```

> 注意结尾要带 `/`。脚本只改文本引用，不动任何功能代码；不执行此步骤网站也完全正常，
> 只是搜索引擎收录地址和微信/微博分享卡片会指向旧地址。

## 四、各服务器配置样例

### Nginx

```nginx
server {
    listen 80;
    server_name your.company.com;
    root /var/www/fde;            # site/ 内容所在目录
    index index.html;
    charset utf-8;

    error_page 404 /404.html;     # 自定义 404（文件已包含在包内）

    # 静态资源缓存（HTML 短缓存，资源长缓存）
    location ~* \.(css|js|png|svg|woff2?)$ { expires 7d; }
    location ~* \.html$ { expires 10m; }

    gzip on;
    gzip_types text/css application/javascript image/svg+xml;
}
```

### IIS（web.config，放到站点根目录）

```xml
<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <system.webServer>
    <httpErrors errorMode="Custom" existingResponse="Replace">
      <remove statusCode="404"/>
      <error statusCode="404" path="/404.html" responseMode="ExecuteURL"/>
    </httpErrors>
    <staticContent>
      <remove fileExtension=".svg"/>
      <mimeMap fileExtension=".svg" mimeType="image/svg+xml"/>
      <clientCache cacheControlMode="UseMaxAge" cacheControlMaxAge="7.00:00:00"/>
    </staticContent>
    <urlCompression doStaticCompression="true"/>
  </system.webServer>
</configuration>
```

### Apache（.htaccess）

```apache
ErrorDocument 404 /404.html
AddDefaultCharset utf-8
<FilesMatch "\.(css|js|png|svg|woff2?)$">
  Header set Cache-Control "max-age=604800"
</FilesMatch>
```

## 五、上线验收清单

- [ ] 打开 `/`（或子路径）能看到序章：$40B → 95% 大数字动画
- [ ] 右上角「目录」按钮能展开作战地图面板，13 章链接可点
- [ ] 任意章节页（如 `/ch05.html`）图表正常渲染（ECharts 本地加载）
- [ ] 手机访问排版正常（响应式已适配 390px 起）
- [ ] 访问一个不存在的地址（如 `/xxx`）返回美化的 404 页
- [ ] 若做了域名替换：查看任一页源码，`og:url` 已是公司域名

## 六、文件清单说明

| 路径 | 说明 |
| --- | --- |
| index.html | 序章（首页入口） |
| ch01.html … ch13.html | 13 个章节页 |
| toolbox.html | 工具箱页 |
| finish.html | 卡册/战绩页（数据存访客本地） |
| 404.html | 自定义 404 页 |
| sitemap.xml / robots.txt | SEO 文件（域名替换脚本会同步更新） |
| assets/ | 样式、脚本、图表库（ECharts 5.5.1）、分享图、favicon |
| hub.html | AI大同学枢纽：先判断 / 看方法 / 开始改 |
| leaders.html | 给负责人的 6 步读法（不走面试章） |
| start.html | 诊断 / 交流 / 咨询入口 |

## 七、转化页（2026-08-13）

站点已接上 AI大同学双路径：

- 序章底部、全站页脚：`我是负责人` → leaders.html；`我想做 FDE` → ch01.html
- 开始改：`start.html`，表单写入飞书 Base；打不开时引导关注公众号「AI大同学」回复关键词
- 表单地址在 `assets/app.js` 的 `FDE_INTAKE`。Cloudflare quick tunnel 重建后必须改这里和 `start.html` 三处链接

验收再加三条：

- [ ] `/hub.html` 三扇门可点
- [ ] `/leaders.html` 6 步读法链到序章、ch03–ch06、工具箱
- [ ] `/start.html` 三张表能打开；页脚有 AI大同学署名

## 八、后续内容更新

内容源文件（Markdown）和构建脚本由产品侧维护，**每次更新会重新交付整包**，
IT 只需覆盖上传全部文件即可（无增量依赖，直接全量替换最稳妥）。
